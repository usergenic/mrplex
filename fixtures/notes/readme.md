# Notes

A tiny sample repo used by the seed script and the M0 tests. Documents in here have no frontmatter — the frontmatter split/parse utility (§3.2) tolerates that and returns an empty parsed object.
